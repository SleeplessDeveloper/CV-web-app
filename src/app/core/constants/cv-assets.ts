/**
 * Location and download name of the attached CV document.
 *
 * The path is deliberately version-free: refreshing the CV should be a file
 * swap, not a code change. The download name omits the year for the same
 * reason — a saved copy should never look stale.
 */
export const CV_PDF_PATH = 'pdf/cv.pdf';
export const CV_PDF_DOWNLOAD_NAME = 'Siphephelo_Sibanyoni_CV.pdf';
